#include <stdio.h>
/**
 * main - this  the alphabet in lowercase, and then in uppercase
 *
 * Return: Always 0.
 */
int main(void)
{
char n;
for (n = 'a'; n <= 'z'; ++n)
putchar(n);
}
char c;
for (c = 'A'; c <= 'Z'; ++n)
{
putchar(c);
}
putchar('\n');
return (0);
}
