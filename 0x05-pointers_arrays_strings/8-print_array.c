#include "holberton.h"

/**
 * print_array-prints n elements of an array of integers
 * @n: variable
 * @a: variable
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int b;

	for (b = 0; b[a] < n ; b++)
	{
		if ((b < (n - 1))
	{
		x = str[n];
		printf(", ");
	}
		printf('\n');
}
