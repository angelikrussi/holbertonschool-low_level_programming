#include "holberton.h"
#include <stdio.h>

/**
 *_strlen - this function that returns the length of a string..
 *@s: variable.
 * Return: Always 1.
 */
int _strlen(char *s)
{
	int le;

	for (le = 0; s[le] != 0; le++)
	{
	}
	return (le);
}
