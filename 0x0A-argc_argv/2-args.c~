#include <stdio.h>
/**
 * main - program that prints all arguments it receives.
 * @argc: argument count
 * @argv: argument vector
 * Return: o.
 */
int main(int argc, char *argv[])
{
	int w = 0;

	for (;w <= argc; w++)
	printf("%s\n", argv[w]);
	return (0);
}
