#include <stdio.h>
/**
 * main - prints from the first occurrence of a char.
 * @argc: argument count
 * @argv: argument vector
 * Return: o.
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
